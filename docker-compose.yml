services:
  os-detector:
    build:
      context: .
      dockerfile: Dockerfile
    image: os-detector:latest
    container_name: os-detector
    ports:
      - "${OS_DETECTOR_PORT:-3000}:3000"
    restart: unless-stopped
